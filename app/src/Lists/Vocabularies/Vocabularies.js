/**
 * @ngdoc module
 * @name Pundit2.Vocabularies
 * @module Pundit2.Vocabularies
 * @description Vocabularies module provide an interface to show items taken from vocabularies server database.
 * It provide a search through text input to query the databases and some filters to display 
 * the items belonging to the same database. It also possible to order the items.
**/
angular.module('Pundit2.Vocabularies', []);